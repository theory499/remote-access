# Firebase configuration

This folder contains the **only** server-side configuration the remote
desktop suite needs:

- `database.rules.json` - Realtime Database security rules. Paste the
  contents into your project's **Realtime Database > Rules** tab.

See `../docs/FIREBASE_SETUP.md` for the full setup walkthrough.
